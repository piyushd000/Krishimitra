def detect_drop_logic(data):
    """
    Sample crop detection logic using AI model (replace with real logic).
    """
    # Example placeholder: simulate crop if "acceleration" < threshold
    threshold = 0.5
    acceleration = data.get("acceleration", 1.0)

    if acceleration < threshold:
        return True
    return False
